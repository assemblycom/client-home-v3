import { conditions } from '@segments/lib/conditions/conditions.schema'
import { segments, segmentsRelations } from '@segments/lib/segments/segments.schema'

// Add schema here when you want to use the relational API
export const schema = {
  segments,
  segmentsRelations,
  conditions,
}
